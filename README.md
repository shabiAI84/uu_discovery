#  Unknown–Unknown Discovery using VLMs

This repo is a minimal, educational for
discovering **unknown–unknowns** in unlabeled image data using:

- CLIP embeddings
- Outlier scoring in embedding space
- HDBSCAN clustering
- TSNE/UMAP visualization
- Simple Streamlit browser for exploring clusters


```bash
pip install -r requirements.txt
